# Environment snapshot used for the experiments reported in the paper.
# Python 3.10, CUDA 12.1, tested on NVIDIA A100 (80 GB).
# Only top-level dependencies are pinned; pip resolves transitive ones.
#
# Install with:
#     pip install -r requirements.freeze.txt

--extra-index-url https://download.pytorch.org/whl/cu121

# --- Core training ---
torch==2.5.1+cu121
transformers==4.57.3
tokenizers==0.22.1
safetensors==0.6.2
peft==0.17.1
accelerate==1.12.0
deepspeed==0.18.3

# --- Data ---
datasets==4.1.1
pyarrow==22.0.0

# --- Evaluation ---
lm_eval==0.4.9.2
rouge_score==0.1.2

# --- Scientific stack ---
numpy==2.2.6
scipy==1.15.3
torchmetrics==1.8.2

# --- Config ---
PyYAML==6.0.3
