task: wmdp
model_family: zephyr-7b-beta

data:
  split: bio

model:
  model_path: ./outputs/wmdp-forget/checkpoints/checkpoint-last
  base_model_path: HuggingFaceH4/zephyr-7b-beta
  tokenizer_path: ./outputs/wmdp-forget/checkpoints/checkpoint-last
  local_files_only: false
  trust_remote_code: true
  torch_dtype: bfloat16

lora:
  enabled: false
  targets: all
  r: 0
  alpha: 32
  dropout: 0.0

method:
  name: reglu
  variant: gd
  init_strategy: rila
  rol_lambda: 0.0
  rol_rank: 128
  rol_targets: all_lora

evaluation:
  batch_size: 1
  max_length: 256
  overwrite: true

runtime:
  output_dir: ./outputs/wmdp-eval
  device: auto
  dry_run: false
